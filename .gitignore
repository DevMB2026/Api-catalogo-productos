node_modules/
.env

# Imágenes subidas localmente (temporal hasta Cloudinary) — se conserva la carpeta
uploads/*
!uploads/.gitkeep

# Configuración local de Claude Code / editor
.claude/

# Artefactos de scraping/migración (regenerables, no versionar)
data/
